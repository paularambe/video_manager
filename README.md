El algoritmo de python corre en la raspi5. El algoritmo en CameraWebServer en la esp32cam.

Activar el venv antes de lanzar



**Instalar venv**
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt